import React from "react"
import ReactDOM from "react-dom/client"
import ChangeState from "./counter"

const root = ReactDOM.createRoot(document.getElementById("root"))

root.render(
  <div>
    <ChangeState></ChangeState>
  </div>
)