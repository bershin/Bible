import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("root"));
// Render can have only one tag
root.render(
    <div>
      <h1>Hello</h1>
      <p>World</p>
    </div>
  );